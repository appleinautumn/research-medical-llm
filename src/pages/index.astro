---
import Layout from '../components/Layout.astro';

// Read the markdown file
const latestMd = await Astro.glob('../pages/docs/latest.md');
const { Content, frontmatter } = latestMd[0];
---

<Layout>
  <h1>{frontmatter.title}</h1>
  <Content />
</Layout>